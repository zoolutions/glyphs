# Glyphs — Claude Code entry point

@AGENTS.md

`AGENTS.md` is the single project-instruction file, shared with any other tool that reads
`AGENTS.md` directly. Claude-only material lives in `.claude/`.
